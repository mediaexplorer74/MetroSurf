<Page
    x:Class="VPN.View.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="LoadedContentGrid">
        <TextBlock x:Name="TextBlockVersion" />
        <TextBlock x:Name="TextBlockYearAndCopyRight" />
        <Canvas x:Name="icn_share" />
        <Canvas x:Name="icn_star" />
        <Canvas x:Name="icn_support" />
        <Canvas x:Name="icn_tutorial" />
        <Canvas x:Name="Your_Icon" />
        <ToggleSwitch x:Name="RememberPasswordToggle" />
        <Canvas x:Name="icn_kid" />
    </Grid>
</Page>
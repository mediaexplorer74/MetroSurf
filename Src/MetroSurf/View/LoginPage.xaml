<Page
    x:Class="VPN.View.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VPN.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot">
        <ProgressBar x:Name="ProgressBar" Visibility="Collapsed"/>
        <ProgressBar x:Name="ProgressBarForAutoLogin" Visibility="Collapsed"/>

        <Grid x:Name="LoginButtons">
            <Canvas x:Name="icn_add" Width="48" Height="48"/>
            <Canvas x:Name="icn_kid" Width="48" Height="48"/>
            <Canvas x:Name="icn_g" Width="48" Height="48"/>
            <Canvas x:Name="icn_f" Width="48" Height="48"/>
            <Canvas x:Name="logo_keepsolid" Width="128" Height="32"/>
        </Grid>
    </Grid>
</Page>